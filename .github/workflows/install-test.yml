name: install-test

on:
  workflow_dispatch:
    inputs:
      repo:
        description: 'GitHub repo to install from (owner/name)'
        required: false
        default: 'fanfeilong/xgoup'
      version:
        description: 'Release tag to install (e.g. v0.1.0) or "latest"'
        required: false
        default: 'latest'
  push:
    paths:
      - 'scripts/install.sh'
      - 'scripts/install.ps1'
      - '.github/workflows/install-test.yml'
  pull_request:
    paths:
      - 'scripts/install.sh'
      - 'scripts/install.ps1'
      - '.github/workflows/install-test.yml'

permissions:
  contents: read

jobs:
  unix:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install (macOS/Linux) via curl | bash (README)
        if: runner.os != 'Windows'
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          raw="https://raw.githubusercontent.com/${GITHUB_REPOSITORY}/${GITHUB_SHA}"
          curl -fsSL "$raw/scripts/install.sh" | XGOUP_GITHUB_REPO="${{ inputs.repo || 'fanfeilong/xgoup' }}" XGOUP_INSTALL_VERSION="${{ inputs.version || 'latest' }}" GITHUB_TOKEN="${GITHUB_TOKEN}" bash

      - name: Install (macOS/Linux) via wget | bash (README)
        if: runner.os != 'Windows'
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          raw="https://raw.githubusercontent.com/${GITHUB_REPOSITORY}/${GITHUB_SHA}"

          if ! command -v wget >/dev/null 2>&1; then
            if [[ "${RUNNER_OS}" == "macOS" ]]; then
              brew update >/dev/null
              brew install wget
            else
              sudo apt-get update -y
              sudo apt-get install -y wget
            fi
          fi

          wget -qO- "$raw/scripts/install.sh" | XGOUP_GITHUB_REPO="${{ inputs.repo || 'fanfeilong/xgoup' }}" XGOUP_INSTALL_VERSION="${{ inputs.version || 'latest' }}" GITHUB_TOKEN="${GITHUB_TOKEN}" bash

      - name: Verify (macOS/Linux)
        shell: bash
        run: |
          set -euo pipefail
          bin="$HOME/.xgoup/bin/xgoup"
          echo "Bin: $bin"
          test -x "$bin"
          "$bin" --version

  windows:
    strategy:
      fail-fast: false
      matrix:
        shell:
          - pwsh       # PowerShell 7+
          - powershell # Windows PowerShell 5.1
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install (Windows / pwsh) via curl.exe (README)
        if: matrix.shell == 'pwsh'
        shell: pwsh
        env:
          XGOUP_GITHUB_REPO: ${{ inputs.repo || 'fanfeilong/xgoup' }}
          GITHUB_TOKEN: ${{ github.token }}
          XGOUP_HOME: ${{ runner.temp }}\xgoup-pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = "Stop"

          $raw = "https://raw.githubusercontent.com/${env:GITHUB_REPOSITORY}/${env:GITHUB_SHA}"
          $url = "$raw/scripts/install.ps1"
          $ver = "${{ inputs.version || 'latest' }}"
          Write-Host "Shell: $($PSVersionTable.PSVersion)"
          Write-Host "Installing via: $url"

          $p = Join-Path $env:TEMP "xgoup-install.ps1"
          curl.exe -fsSL $url -o $p
          pwsh -NoProfile -ExecutionPolicy Bypass -File $p -Version $ver

      - name: Verify (Windows / pwsh)
        if: matrix.shell == 'pwsh'
        shell: pwsh
        env:
          XGOUP_HOME: ${{ runner.temp }}\xgoup-pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = "Stop"

          $bin = Join-Path $env:XGOUP_HOME "bin"
          Write-Host "Bin dir: $bin"

          if (Test-Path (Join-Path $bin "xgoup.exe")) {
            $cmd = (Join-Path $bin "xgoup.exe")
          } elseif (Test-Path (Join-Path $bin "xgoup.cmd")) {
            $cmd = (Join-Path $bin "xgoup.cmd")
          } else {
            throw "xgoup not found in $bin"
          }

          $out = & $cmd --version 2>&1
          $code = $LASTEXITCODE

          $text = ($out | Out-String).Trim()
          Write-Host $text
          Write-Host "Exit code: $code"

          if ($text -match 'xgoup(-windows-wrapper)?\s+\S+') {
            exit 0
          }

          throw "Unexpected output from --version (exit=$code): $text"

      - name: Install (Windows / powershell) via curl.exe (README)
        if: matrix.shell == 'powershell'
        shell: powershell
        env:
          XGOUP_GITHUB_REPO: ${{ inputs.repo || 'fanfeilong/xgoup' }}
          GITHUB_TOKEN: ${{ github.token }}
          XGOUP_HOME: ${{ runner.temp }}\xgoup-powershell
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = "Stop"

          $raw = "https://raw.githubusercontent.com/${env:GITHUB_REPOSITORY}/${env:GITHUB_SHA}"
          $url = "$raw/scripts/install.ps1"
          $ver = "${{ inputs.version || 'latest' }}"
          Write-Host "Shell: $($PSVersionTable.PSVersion)"
          Write-Host "Installing via: $url"

          $p = Join-Path $env:TEMP "xgoup-install.ps1"
          curl.exe -fsSL $url -o $p
          powershell -NoProfile -ExecutionPolicy Bypass -File $p -Version $ver

      - name: Verify (Windows / powershell)
        if: matrix.shell == 'powershell'
        shell: powershell
        env:
          XGOUP_HOME: ${{ runner.temp }}\xgoup-powershell
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = "Stop"

          $bin = Join-Path $env:XGOUP_HOME "bin"
          Write-Host "Bin dir: $bin"

          if (Test-Path (Join-Path $bin "xgoup.exe")) {
            $cmd = (Join-Path $bin "xgoup.exe")
          } elseif (Test-Path (Join-Path $bin "xgoup.cmd")) {
            $cmd = (Join-Path $bin "xgoup.cmd")
          } else {
            throw "xgoup not found in $bin"
          }

          $out = & $cmd --version 2>&1
          $code = $LASTEXITCODE

          $text = ($out | Out-String).Trim()
          Write-Host $text
          Write-Host "Exit code: $code"

          if ($text -match 'xgoup(-windows-wrapper)?\s+\S+') {
            exit 0
          }

          throw "Unexpected output from --version (exit=$code): $text"

